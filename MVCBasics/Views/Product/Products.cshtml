@model List<InventoryDomain.Models.ListableProduct>

@{
    ViewBag.Title = "Products";
    ViewBag.Active = "Product";
}

<h2>@ViewBag.Title</h2>

<div class="col-md-1">
</div>  
<div class="col-md-10">
<table id="productsTable" class="table table-striped table-bordered table-condensed">
    <thead>
        <tr>
            <th>
                Product Code
            </th>
            <th>
                Price
            </th>
            <th>
                In Stock
            </th>
        </tr>
    </thead>
    <tbody>
        
        @foreach (var product in Model)
        {
            <tr>
                <td>
                    @product.Sku
                </td>
                <td>
                    @product.Price
                </td>
                <td>
                    <input type="checkbox"  checked="@product.HasStock" />
                </td>
                <td>
                        @Html.ActionLink("Edit", "Index", "ProductDetail", new{id = 2}, new {@class = "btn btn-primary"})
                </td>
            </tr>
        }
    </tbody>
</table>
</div>
<div class="col-md-1">
</div>  
<div class="col-md-12">
<ul>
    <li>
        @Html.ActionLink("Product Detail", "Index", "ProductDetail", new { id = 1}, null);
    </li>
</ul>
</div>